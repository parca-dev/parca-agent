.idea
/goreleaser
/dist
/target
/tmp
/out
/bin
*.bpf.o
# bpf2go output, all regenerated by `make probes-bpf`. The .go wrapper embeds
# the .o, so committing one without the other only lets the wrapper go stale.
probes/probe_bpfel.go
probes/probe_bpfel.o
probes/probe_bpfeb.go
probes/probe_bpfeb.o
probes/bpf/*.ll
TODO.md
minikube-*
/data

# Snap Packaging Artifacts
*.snap
snap/local/parca-agent
snap/local/metadata.json

# kernel test stuff
kerneltest/*.test
kerneltest/initramfs.cpio
kerneltest/logs/vm_log_*.txt
kerneltest/kernels/linux-*.bz
kerneltest/ci-kernels

# direnv / Nix
.direnv
.envrc
result*

# debug logs
*.log
*.plan
*.token

# java
*.class
